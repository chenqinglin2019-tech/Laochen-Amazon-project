# Amazon Niche Choice v2（Agent 入口）

完整指令在 `INSTRUCTIONS.md`。

## 概要

用户给 niche 名称列表 → CLI 调后端拉数据 + 本地评分 → 你解读结果给建议。

## 工具

- `tools/bin/amazon-niche-choice-v2-*`：CLI（score / score-local / health）
- `scoring/*.yaml`：4 套评分 profile

## 优先动作

读 `INSTRUCTIONS.md`。
